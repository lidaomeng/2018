spring:
  application:
    name: service-turbine
server:
  port: 8769
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  security:
    enabled: false
turbine:
  appConfig: eureka-client-one
  clusterNameExpression: new String("default")